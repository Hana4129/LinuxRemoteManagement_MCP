[project]
name = "linux-remote-management-mcp"
version = "0.1.0"
description = "Linux Remote Management MCP Server (管理コンソール + MCP Tools)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
filterwarnings = ["ignore::DeprecationWarning"]
