# Python
__pycache__/
*.pyc
.pytest_cache/

# 仮想環境 (git 管理外 — setup.sh / start_server.sh が自動構築する)
.venv/

# 実行時データ（トークンDB等） - 絶対Gitにコミットしない
data/
*.db
*.db-wal
*.db-shm
.env

# ログ
*.log

# 本番設定（secrets含むためgit追跡外）
config.prd.yml
